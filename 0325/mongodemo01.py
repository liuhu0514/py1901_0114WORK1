import pymongo

# pymongo.MongoClient()['school']['stu'].update_one({'name': 'liuhu'}, {'$inc': {'age': 30}})
# r = pymongo.MongoClient()['school']['stu'].insert_one({'name': 'liu', 'age': 20})
# print(r)
# pymongo.MongoClient()['admin'].authenticate('root', '123456')
# r = pymongo.MongoClient()['school']['stu'].update_one({'name': 'liuhu'}, {'$set': {'age': 30}})
# print(r)
# con = pymongo.MongoClient()['goods'].authenticate('temp1', 'temp1')
# con['class'].update
